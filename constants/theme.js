import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#445FAD',
    accent: '#d62400'
    // error: '#f13a59',
    // success: '#00B386'
  }
};

export default theme;
